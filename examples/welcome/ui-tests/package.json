{
  "name": "@midnight-ntwrk/welcome-ui-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint ./src",
    "proof-server": "docker compose -f proof-server.yml pull && docker compose -f proof-server.yml up",
    "allure-generate": "allure generate ./reports/allure-results --clean -o ./reports/allure-report",
    "smoke-test": "yarn playwright test /src/tests/smoke"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "allure-commandline": "^2.27.0",
    "allure-playwright": "^2.15.0",
    "dotenv": "^16.4.5",
    "eslint": "^8.57.0",
    "eslint-config-standard-with-typescript": "^39.1.1",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "pino": "^9.1.0",
    "pino-pretty": "^11.1.0"
  }
}
